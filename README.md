# Juno Nextflow Template
Demonstration / template for running nextflow workflows on the MSKCC Juno cluster

## Usage

1. Clone this repo
2. `./juno_nextflow.sh run examples/example.nf`
3. `./juno_nextflow.sh run examples/example_gpu.nf`